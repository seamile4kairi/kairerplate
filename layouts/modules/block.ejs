<% block = ($content) => {
  if (typeof $content === 'string') return block
  if (typeof $content !== 'function') return ''

  const arr = []
  $content(txt => arr.push(txt))
  return arr.join('')
} -%>
