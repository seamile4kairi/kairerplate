<%
block = (block, fallback = '') => {
  if (typeof block === 'string') return block
  if (typeof block !== 'function') return fallback

  const arr = []
  block(txt => arr.push(txt))
  return arr.join('')
}
%>
